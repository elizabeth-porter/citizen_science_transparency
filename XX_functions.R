# Data Manuipulation
library(dplyr)
library(tidyverse)

# Figures
library(ggplot2)
library(viridis)

# Settings
theme_set(theme_light())

# to extract R code from .qmd files, run the following line
knitr::purl("file of choice.qmd")
